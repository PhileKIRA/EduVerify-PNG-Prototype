{
  "name": "eduverify-png-sevispass-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend proxy holding the SevisPass client secret server-side (see ../src/application/sevisAuth.js)",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
